# Copy this file to `.env` to override the defaults.
# These values match the Smart Parking API server.
VITE_API_BASE_URL=http://localhost:8084/ambient-invisible-intelligence
VITE_SYSTEM_ID=2026b.Omer.Asayag
VITE_API_VERSION=1.3
# Optional fallback identity for tooling; the app itself uses the signed-in
# user (see the /login and /signup screens) for all API calls.
#VITE_USER_EMAIL=admin@demo.org
